[project]
name = "patina-runs"
version = "0.1.0"
description = "Experiment tracking and plotting utilities for PATINA runs."
requires-python = ">=3.11"
dependencies = [
    "gudhi>=3.12",
    "marimo[recommended]>=0.23.4",
    "matplotlib>=3.8",
    "numpy>=2.0",
    "pandas>=2.2",
    "plotly>=6.0",
]

[tool.uv]
package = false
